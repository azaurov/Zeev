[pytest]
# Only tests/ is collected. zeev/test_sqlite_migration.py and
# zeev/test_call_personas.py are NOT pytest tests despite the name — they are
# standalone scripts (the latter places real phone calls). Do not add zeev here.
testpaths = tests
addopts = -v --tb=short
